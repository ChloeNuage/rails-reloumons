<nav>
  <ul>
    <li><%= link_to "Accueil", root_path %></li>
    <li><%= link_to "Ma Collection", collection_user_path(current_user) if user_signed_in? %></li>
    <% if user_signed_in? %>
      <li><%= link_to "Se Déconnecter", destroy_user_session_path, method: :delete %></li>
    <% else %>
      <li><%= link_to "Se Connecter", new_user_session_path %></li>
    <% end %>
  </ul>
</nav>

<h1>Reloumons</h1>

<div class="cards-container">
  <% @cards.each do |card| %>
    <div class="card">
      <%= image_tag card.photo if card.photo.attached? %>
      <h2><%= link_to card.name, card_path(card) %></h2>
    </div>
  <% end %>
</div>
